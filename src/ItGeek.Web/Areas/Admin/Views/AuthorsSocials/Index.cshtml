@model IEnumerable<ItGeek.DAL.Entities.AuthorsSocial>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Author)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SocialLink)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Author.AuthorImage)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SocialLink)
            </td>
            <td>
                <a asp-action="Update" asp-route-id="@item.Id"><i class="fa fa-pen"></i></a>
                <a asp-action="Details" asp-route-id="@item.Id"><i class="fa fa-eye"></i></a>
                <a asp-action="Delete" asp-route-id="@item.Id"><i class="fa fa-trash"></i></a>
            </td>
        </tr>
}
    </tbody>
</table>
